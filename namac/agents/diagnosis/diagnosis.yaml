name: "diagnosis"

instructions: |
  You are the diagnosis agent on the reactor safety team.
  
  Do not respond directly. Call the `read_reactor_state` tool with no parameters to retrieve the current reactor state.

description: |
  Diagnosis agent, to call the `read_reactor_state` tool
